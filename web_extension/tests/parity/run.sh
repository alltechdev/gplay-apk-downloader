#!/usr/bin/env bash
# Parity test: drive the legacy Python CLI (in the venv) against a known package,
# capture its on-wire requests + responses, then run the JS port and compare.
# Empty until the JS port lands — exits 0 with a notice.
set -euo pipefail
echo "[parity] not yet wired — JS port not implemented"
exit 0
