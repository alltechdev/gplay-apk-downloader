// Placeholder so `npm test` exits 0 before any real units exist.
// First real unit test replaces this file.
import { test } from 'node:test';
import assert from 'node:assert/strict';

test('placeholder: node:test runner works', () => {
  assert.equal(1 + 1, 2);
});
